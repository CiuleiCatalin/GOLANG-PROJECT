I attached the "output_example" already but to see how the application runs, just delete the "output_example and run main.go  so it creates another "output_example". You will need the "input_example" to be in the folder aswell
